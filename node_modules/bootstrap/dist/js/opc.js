function print_form_inputs() {
    var the_form = document.getElementById('build_config');
    var txt = "";
    var i;

    for ( i = 0; i < the_form.length; i++) {
        txt = txt + the_form.elements[i];
    }
    document.getElementById('review_inputs').innerHTML = txt
}
